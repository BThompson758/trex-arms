export class ItemModel {
    img: string;
    name: string;
    num: number;

    constructor(img: string, name: string, num: number) {
        this.img = img;
        this.name = name;
        this.num = num;
    }
}